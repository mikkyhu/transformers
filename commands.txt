# rsync file from local to PNLP

rsync -avz generate_with_entropy.py pnlp:/n/fs/pnlp/myhu/transformers/examples/

# generate_with_entropy

python3 generate_with_entropy.py --model_type=gpt2 --length=10 --model_name_or_path=gpt2 --prompt="I like cats."

# generate with calibration

python3 generate_with_calibration.py --model_type=gpt2 --length=20 --model_name_or_path=gpt2 --prompt='In a shocking finding, scientist discovered a herd of unicorns living in a remote, previously unexplored valley, in the Andes Mountains. Even more surprising to the researchers was the fact that the unicorns spoke perfect English' --alpha=1.27 --batch_size=64


# get a hold of gpu for testing before script run

gonode --gres=gpu:1 --mem=16G